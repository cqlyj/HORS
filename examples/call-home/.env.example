# --- Required for server ---
OWNER_HUMAN_ID=0x...
HORS_DOMAIN=openagents.eth
PORT=3200
HORS_STATE_KEY=call-home-dev-key-min-32-bytes!!!

# --- Required for on-chain token transfers ---
OWNER_PRIVATE_KEY=0x...

# --- Required for 0G verified inference ---
OG_ROUTER_API_KEY=sk-...
OG_MODEL=qwen2.5-omni

# --- Required for selfie step-up (home.borrow) ---
WORLD_RP_ID=rp_...
WORLD_SIGNING_KEY=0x...
WORLD_APP_ID=app_...

# --- Required for `make setup` (enroll.ts) ---
ENS_NAME=openagents.eth
STORAGE_SIGNER_PRIVATE_KEY=0x...

# --- Generated by `make setup` ---
HORS_SERVICE_ID=

# --- Optional: allow ngrok/tunnel Host headers (if not using `make expose` rewrite) ---
# ALLOWED_HOSTS=xxxx.ngrok-free.app
