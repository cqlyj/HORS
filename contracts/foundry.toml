[profile.default]
src = "src"
out = "out"
libs = ["lib"]
script = "script"

solc = "0.8.24"
evm_version = "cancun"
auto_detect_solc = false
optimizer = true
optimizer_runs = 200

extra_output_files = ["abi"]

[rpc_endpoints]
galileo = "https://evmrpc-testnet.0g.ai"

# Verification (use explicit flags; chain 16602 is not in Foundry 1.5.x built-in registry)
# API base: https://chainscan-galileo.0g.ai/open/api  (not /api)
